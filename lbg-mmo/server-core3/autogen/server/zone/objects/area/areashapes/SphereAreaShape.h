/*
 *	autogen/server/zone/objects/area/areashapes/SphereAreaShape.h generated by engine3 IDL compiler 0.70
 */

#ifndef SPHEREAREASHAPE_H_
#define SPHEREAREASHAPE_H_

#include "engine/core/Core.h"

#include "engine/core/ManagedReference.h"

#include "engine/core/ManagedWeakReference.h"

#include "system/util/Optional.h"

#ifndef likely
#ifdef __GNUC__
#define likely(x)       __builtin_expect(!!(x), 1)
#define unlikely(x)     __builtin_expect(!!(x), 0)
#else
#define likely(x)       (x)
#define unlikely(x)     (x)
#endif
#endif
#include "engine/util/json_utils.h"

namespace server {
namespace zone {
namespace objects {
namespace area {
namespace areashapes {

class CuboidAreaShape;

class CuboidAreaShapePOD;

} // namespace areashapes
} // namespace area
} // namespace objects
} // namespace zone
} // namespace server

using namespace server::zone::objects::area::areashapes;

#include "engine/util/u3d/Vector3.h"

#include "system/lang/Math.h"

#include "server/zone/objects/area/areashapes/AreaShape.h"

namespace server {
namespace zone {
namespace objects {
namespace area {
namespace areashapes {

class SphereAreaShape : public AreaShape {
public:
	SphereAreaShape();

	void setRadius(float rad);

	float getArea();

	float getRadius() const;

	float getRadius2() const;

	/**
	 * Check if the coordinate is within the area shape.
	 * @param x the x coordinate.
	 * @param y the y coordinate.
	 * @param z the z coordinate.
	 * @return true if the coordinate is within the area shape.
	 */
	bool containsPoint(float x, float z, float y) const;

	/**
	 * Check if the coordinate is within the area shape.
	 * @param point the point to check if it is within the area shape.
	 * @return true if the coordinate is within the area shape.
	 */
	bool containsPoint(const Vector3& point) const;

	/**
	 * Generate a random position within the area.
	 * @return a random position within the area.
	 */
	Vector3 getRandomPosition() const;

	/**
	 * Generate a random position within the area with the supplied origin and radius as limits.
	 * @param origin the origin coordinate to generate the random position from.
	 * @param minDistance the minimum distance from the origin.
	 * @param maxDistance the maximum distance from the origin.
	 * @return a random position within the area.
	 */
	Vector3 getRandomPosition(const Vector3& origin, float minDistance, float maxDistance) const;

	/**
	 * Check if this area shape intersects with the supplied area shape.
	 * @param areaShape the area shape to check for intersections with.s
	 * @return true if the area shapes intersects each other.
	 */
	bool intersectsWith(AreaShape* areaShape) const;

	/**
	 * Check if this is a sphere area shape.
	 * @return true if it is a sphere area shape.
	 */
	bool isSphereAreaShape() const;

	DistributedObjectServant* _getImplementation();
	DistributedObjectServant* _getImplementationForRead() const;

	void _setImplementation(DistributedObjectServant* servant);

protected:
	SphereAreaShape(DummyConstructorParameter* param);

	virtual ~SphereAreaShape();

	friend class SphereAreaShapeHelper;
};

} // namespace areashapes
} // namespace area
} // namespace objects
} // namespace zone
} // namespace server

using namespace server::zone::objects::area::areashapes;

namespace server {
namespace zone {
namespace objects {
namespace area {
namespace areashapes {

class SphereAreaShapeImplementation : public AreaShapeImplementation {
protected:
	float radius;

	/** The squared radius of the circle */
	float radius2;

public:
	SphereAreaShapeImplementation();

	SphereAreaShapeImplementation(DummyConstructorParameter* param);

	void setRadius(float rad);

	float getArea();

	float getRadius() const;

	float getRadius2() const;

	/**
	 * Check if the coordinate is within the area shape.
	 * @param x the x coordinate.
	 * @param y the y coordinate.
	 * @param z the z coordinate.
	 * @return true if the coordinate is within the area shape.
	 */
	bool containsPoint(float x, float z, float y) const;

	/**
	 * Check if the coordinate is within the area shape.
	 * @param point the point to check if it is within the area shape.
	 * @return true if the coordinate is within the area shape.
	 */
	bool containsPoint(const Vector3& point) const;

	/**
	 * Generate a random position within the area.
	 * @return a random position within the area.
	 */
	Vector3 getRandomPosition() const;

	/**
	 * Generate a random position within the area with the supplied origin and radius as limits.
	 * @param origin the origin coordinate to generate the random position from.
	 * @param minDistance the minimum distance from the origin.
	 * @param maxDistance the maximum distance from the origin.
	 * @return a random position within the area.
	 */
	Vector3 getRandomPosition(const Vector3& origin, float minDistance, float maxDistance) const;

	/**
	 * Check if this area shape intersects with the supplied area shape.
	 * @param areaShape the area shape to check for intersections with.s
	 * @return true if the area shapes intersects each other.
	 */
	bool intersectsWith(AreaShape* areaShape) const;

private:
	bool intersectsWithSphere(SphereAreaShape* sphere) const;

	bool intersectsWithCuboid(CuboidAreaShape* cuboid) const;

public:
	/**
	 * Check if this is a sphere area shape.
	 * @return true if it is a sphere area shape.
	 */
	bool isSphereAreaShape() const;

	WeakReference<SphereAreaShape*> _this;

	operator const SphereAreaShape*();

	DistributedObjectStub* _getStub();
	virtual void readObject(ObjectInputStream* stream);
	virtual void writeObject(ObjectOutputStream* stream);
protected:
	virtual ~SphereAreaShapeImplementation();

	void finalize();

	void _initializeImplementation();

	void _setStub(DistributedObjectStub* stub);

	void lock(bool doLock = true);

	void lock(ManagedObject* obj);

	void rlock(bool doLock = true);

	void wlock(bool doLock = true);

	void wlock(ManagedObject* obj);

	void unlock(bool doLock = true);

	void runlock(bool doLock = true);

	void _serializationHelperMethod();
	bool readObjectMember(ObjectInputStream* stream, const uint32& nameHashCode);
	int writeObjectMembers(ObjectOutputStream* stream);

	friend class SphereAreaShape;
};

class SphereAreaShapeAdapter : public AreaShapeAdapter {
public:
	SphereAreaShapeAdapter(SphereAreaShape* impl);

	void invokeMethod(sys::uint32 methid, DistributedMethod* method);

	void setRadius(float rad);

	float getArea();

	float getRadius() const;

	float getRadius2() const;

	bool containsPoint(float x, float z, float y) const;

	bool intersectsWith(AreaShape* areaShape) const;

	bool isSphereAreaShape() const;

};

class SphereAreaShapeHelper : public DistributedObjectClassHelper, public Singleton<SphereAreaShapeHelper> {
	static SphereAreaShapeHelper* staticInitializer;

public:
	SphereAreaShapeHelper();

	void finalizeHelper();

	DistributedObject* instantiateObject();

	DistributedObjectPOD* instantiatePOD();

	DistributedObjectServant* instantiateServant();

	DistributedObjectAdapter* createAdapter(DistributedObjectStub* obj);

	friend class Singleton<SphereAreaShapeHelper>;
};

} // namespace areashapes
} // namespace area
} // namespace objects
} // namespace zone
} // namespace server

using namespace server::zone::objects::area::areashapes;

namespace server {
namespace zone {
namespace objects {
namespace area {
namespace areashapes {

class SphereAreaShapePOD : public AreaShapePOD {
public:
	Optional<float> radius;

	Optional<float> radius2;

	String _className;
	SphereAreaShapePOD();
	virtual void readObject(ObjectInputStream* stream);
	virtual void writeObject(ObjectOutputStream* stream);
	bool readObjectMember(ObjectInputStream* stream, const uint32& nameHashCode);
	int writeObjectMembers(ObjectOutputStream* stream);
	void writeObjectCompact(ObjectOutputStream* stream);



	virtual ~SphereAreaShapePOD();

};

} // namespace areashapes
} // namespace area
} // namespace objects
} // namespace zone
} // namespace server

using namespace server::zone::objects::area::areashapes;

#endif /*SPHEREAREASHAPEPOD_H_*/
