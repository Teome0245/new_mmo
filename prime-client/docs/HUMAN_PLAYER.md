# Human Player

## Launchpad Windows (WSL prime-client)

Ops (2026-07-27).

- `launchpad.config.json` : chemin WSL **prioritaire** vers `prime-client`.
- Sync lecteur J: : `tools/sync_prime_client_to_j.sh` (WSL).
- Vérif Windows : `tools/verify_launchpad_prime.ps1`.
- WS gateway : `ws://192.168.0.246:50000` · compte test Teome.

