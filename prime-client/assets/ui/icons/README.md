# Icônes HUD Prime (flat)

SVG 64×64, palette space western / steampunk (`STYLE_PRIME_UI.md`).

| Fichier | Usage |
|---------|--------|
| `atk`…`menu` | Hotbar slots 1–0 |
| `crate` `ration` `kit` `copper` `screw` `badge` `card` | Inventaire stub |
| `empty` | Slot vide |

Chargement : `UiIconRegistry.tex(name)`.
